#include "main.h"

/**
 * mul - multiply two digits.
 * @a: first digit.
 * @b: second digit
 *
 * Return: result of multiplication.
 */
int mul(int a, int b)
{
	int result = a * b;

	return (result);
}
