#include <stdio.h>

/**
 * _abs - Returns the absolute vale of a number
 * @n: The character to be formatted
 *
 * Return: The absolutee value of (n)
 */
int _abs(int n)
{
	int value = n * ((n > 0) - (v < 0));

	return (value);
}
